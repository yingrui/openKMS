# Copy to .env — must match backend OPENKMS_AUTH_MODE (default dev: local).
OPENKMS_AUTH_MODE=local
OPENKMS_CLI_BASIC_USER=openkms-cli
OPENKMS_CLI_BASIC_PASSWORD=change-me

# OIDC client credentials when OPENKMS_AUTH_MODE=oidc (token URL or base URL + realm + secret).
# OPENKMS_OIDC_TOKEN_URL=
# OPENKMS_OIDC_AUTH_SERVER_BASE_URL=http://localhost:8081
# OPENKMS_OIDC_REALM=openkms
# OPENKMS_OIDC_SERVICE_CLIENT_ID=openkms-cli
# OPENKMS_OIDC_SERVICE_CLIENT_SECRET=

# S3/MinIO (pipeline upload)
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_ENDPOINT_URL=http://localhost:9000
AWS_BUCKET_NAME=openkms

# Backend API base (required for public-config merge). If OPENKMS_VLM_URL / OPENKMS_VLM_MODEL are
OPENKMS_API_URL=http://localhost:8102
# Optional overrides (omit both URL and model to use public-config defaults):
# OPENKMS_VLM_URL=http://localhost:8101
OPENKMS_VLM_MODEL=PaddlePaddle/PaddleOCR-VL-1.5
# OPENKMS_VLM_API_KEY=