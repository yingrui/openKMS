[project]
name = "ontology-function-service"
version = "0.1.0"
description = "Sandbox executor for openKMS Ontology Functions"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
]

[project.scripts]
ofs = "ofs.main:app"

[tool.uv]
package = false
